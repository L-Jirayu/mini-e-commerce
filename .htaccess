RewriteEngine On

# map /health -> health.php
RewriteRule ^health$ health.php [L]

# (กฎเดิมอื่น ๆ ถ้ามี)
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^ index.php [L]
